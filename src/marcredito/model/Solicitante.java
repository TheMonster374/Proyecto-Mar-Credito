package marcredito.model;

// Solicitante: solicita préstamos y gestiona sus pagos.
public class Solicitante extends Usuario {
    public Solicitante(String id, String nombre, String correo) {
        super(id, nombre, correo);
    }
}
